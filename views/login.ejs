<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-r from-red-500 via-yellow-500 to-green-500 h-screen flex justify-center items-center">
    <div class="bg-white p-10 rounded-2xl shadow-2xl w-full max-w-lg transform transition-all duration-300 hover:scale-105">
        <h2 class="text-4xl font-extrabold text-center text-gray-900 mb-8">Welcome Back</h2>
        
        <form action="/login" method="POST" class="space-y-6">
            <div>
                <label for="email" class="block text-sm font-semibold text-gray-700">Email Address</label>
                <input type="email" id="email" name="email" required
                    class="w-full px-6 py-3 mt-2 border-2 border-gray-300 rounded-lg focus:ring-4 focus:ring-blue-400 text-gray-800">
            </div>

            <div>
                <label for="password" class="block text-sm font-semibold text-gray-700">Password</label>
                <input type="password" id="password" name="password" required
                    class="w-full px-6 py-3 mt-2 border-2 border-gray-300 rounded-lg focus:ring-4 focus:ring-blue-400 text-gray-800">
            </div>

            <div class="flex justify-between items-center">
                <label class="flex items-center text-sm text-gray-600">
                    <input type="checkbox" class="mr-2 text-blue-500"> Remember Me
                </label>
                <a href="#" class="text-sm text-blue-600 hover:text-blue-800 font-semibold">Forgot Password?</a>
            </div>

            <button type="submit"
                class="w-full py-3 px-4 bg-gradient-to-r from-blue-500 to-teal-500 text-white font-bold rounded-lg hover:opacity-90 focus:ring-4 focus:ring-blue-600 transition-all duration-300">
                Log In
            </button>
        </form>

        <div class="mt-6 text-center">
            <p class="text-sm text-gray-600">Don't have an account? <a href="/register"
                    class="text-blue-600 hover:text-blue-800 font-semibold">Sign up</a></p>
        </div>
    </div>
</body>
</html>
